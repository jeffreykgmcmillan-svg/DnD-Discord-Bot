py-cord[voice] @ git+https://github.com/Pycord-Development/pycord.git@fix/voice-rec-2
# NOTE: as of Aug 2026, the officially released py-cord does NOT support recording
# audio in voice channels due to Discord's DAVE encryption rollout (see
# https://github.com/Pycord-Development/pycord/issues/3139). The line above installs
# directly from an in-progress development branch that fixes this. Do NOT change
# this to a plain "py-cord[voice]==x.y.z" line until that issue is officially closed
# and a new release ships with the fix, or recording will break again.
faster-whisper==1.0.3
requests==2.32.3
anthropic==0.39.0
python-dotenv==1.0.1
aiosqlite==0.20.0
PyNaCl==1.5.0
